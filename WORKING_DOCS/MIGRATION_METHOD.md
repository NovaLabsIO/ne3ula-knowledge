# NE3ULA Migration Method

Archive documents are treated as source material.

Migration pipeline:

archive fragment
→ interpret meaning
→ identify ontology destination
→ merge with related concepts
→ rewrite clearly
→ place in WORKING_DOCS

Important rule:

archive structure ≠ final structure

Multiple archive files may merge into one canonical document.

The goal is conceptual clarity, not preservation of file boundaries.
